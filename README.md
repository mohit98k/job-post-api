1. initialize the express app , type:module for es syntax

2. install nodemon 

3. install prisma

4. write the schema and migrate 

5. set up prisma client @src/prisma.js

6. set up @src/index.js (load env , connect db , start the server on a port )
   (its the launcher)

7. set up @src/app.js   (create the express app , register middleware , register routes)
   (its the application)